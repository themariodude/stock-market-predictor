# Single runtime dependency list for the backend.
# Docker installs from THIS file only. Per-story requirements files cannot be
# composed — merge them here and delete them.

fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
pydantic==2.10.4
pydantic-settings==2.7.0

# Market data / analysis
yfinance==0.2.51
pandas==2.2.3
numpy==2.2.1
